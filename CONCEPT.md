# CONCEPT.md

## 1. MAC (Multiply-Accumulate, 곱셈-누산)
- 두 수를 곱한 뒤, 그 결과를 기존값에 더하는 과정
- A <- A + (B × C)


- 왜 사용할까?
    - 인공지능 연산의 기본은 행렬곱. 행렬곱은 많은 MAC 연산의 집합 (y = Wx + b)
    - 반복 계산에 적합하고 효율적임
        - 입력값이 여러 개일 때, 더하기만 하는 것보다, 각 입력에 서로 다른 중요도를 반영할 수 있음. 
        - ⓔ (입력값 1 * 중요도 1) + (입력값 2 * 중요도 2) + ...

---


## 2. NPU (Neural Processing Unit, 신경망 처리 장치)
- 인공신경망을 처리하기 위해 특수하게 설계된 전용 하드웨어

### 2.1 CPU VS GPU VS NPU
| 구분 | CPU | GPU | NPU |
| :---: | :---: | :---: | :---: |
| 주요 역할 | 컴퓨터 전체 제어 및 복잡한 연산 | 그래픽 출력 및 AI 학습  |  AI 및 딥러닝 추론 최적화 |
| 장점 | 범용성 | 병렬 처리| 효율성, 전력소모 낮음|
| 구조 특성 | 소수의 강렬한 코어 (2~16개) | 수천 개의 작은 코어 | AI 연산(MAC) 전용 회로 집적 |
| 계산 방식 | 직렬 | 병렬 | 병렬 + AI 특화 최적화 |
| 비유 | 교수님 1명 | 초등학생 3,000명 |  AI 전용 계산기 |

- CPU: 컴퓨터의 두뇌
- GPU: 원래는 게임 화면 그리기 위해 만들어짐. 화면의 수만 개 픽셀을 동시에 계산해야하기 때문에 병렬 연산이 뛰어남. 하지만 전기 많이 먹고, 키가 큼
- NPU: GPU에서 AI 연산에 필요 없는 기능은 다 뺴고, 오직 AI(MAC 연산 등)만 잘하도록 만든 장치. 크기가 작고, 전기를 적게 먹어서 스마트폰 같은 폰에 많이 탑재됨.

### 2.2 왜 NPU 열풍이 아니라 GPU 열풍일까
- 현재 시장에서 GPU 열풍이 부는 이유는 AI 시장이 현재 '추론(실행)'보다 '학습(개발)'단계에 집중되어있기 때문임

1. 범용성, 유연성
- 유연한 GPU: 구조가 유연해서 어떤 형태의 AI 모델 알고리즘 등이 새로 나와도 SW 수정만으로 모두 학습, 연산 가능
- 고정적인 NPU: NPU는 특정 연산 방식을 HW 칩에 미리 새겨 넣은 구조임. 새로운 AI 알고리즘을 돌릴 수 없는 구조

2. 학습 VS 추론
- 학습 특화인 GPU: 수천억 개의 파라미터를 가진 LLM을 훈련하려면 수많은 GPU를 동원해 병렬로 처리해야 함. GPU는 병렬처리에 강함
- 추론 특화인 NPU: 이미 만들어진 AI를 스마트폰, 노트북 등에서 가볍고 빠르게 결과만 도출(추론)하는 건 NPU가 최적화되어 있음. 하지만 거대한 AI를 처음부터 가르치는 '학습'능력은 부족함

3. 독점적인 생태계: CUDA
- GPU가 AI 시장을 장악한 결정적인 이유: 엔비디아의 'CUDA'라는 소프트웨어 플랫폼. 
- CUDA: 엔비디아 GPU를 효율적으로 제어하고 활용할 수 있는 프로그래밍 플랫폼
- GPU는 CUDA 덕분에 AI 연구자들에게 사실상 표준 장비로 자리잡음. 지금도 수많은 연구자들이 CUDA 기반의 라이브러리와 도구에 익숙해져 있어, 갑자기 다른 NPU로 바꾸기 어려움




## 에러 처리